<% provide(:title, 'ユーザー一覧') %>
<h2 class="text-reset"><small>ユーザー一覧</small></h2>
 
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>