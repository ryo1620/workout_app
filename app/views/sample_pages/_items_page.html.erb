<div class="list-group list">
  <% @items.each do |item| %>
   <%= 
     link_to item.name,
     item_path(item_id: item.id),
     class: "list-group-item list-group-item-action"
   %>
  <% end %>
</div>
<%= paginate @items, remote: true %>